# SaS CLI — Errantry coverage

_Generated by `scripts/coverage-report.mjs`. Edit scenario `metadata.exercises` lists to update; do not hand-edit this file._

## Summary

- **Actions registered**: 72
- **Actions covered by ≥1 scenario**: 15 (21%)
- **Actions with no scenario yet**: 57
- **Total scenarios**: 15
- **Scenarios missing `metadata.exercises`**: 0

## Covered actions

| Action | Scenario(s) | Vague prompt(s) |
| --- | --- | --- |
| `scene_create` | [happy-path-create-scene](scenarios/sas/scene/happy-path-create.yaml)<br>[scene-missing-required-arg](scenarios/sas/scene/missing-required-arg.yaml)<br>[scene-name-collision](scenarios/sas/scene/name-collision.yaml)<br>[scene-wrong-project-bound](scenarios/sas/scene/wrong-project-bound.yaml) | "Create a new scene called "Verse" in the currently-bound project."<br>"Create a new scene. The user did not specify a name — pick a sensible default"<br>"Create a new scene called "Intro" in the currently-bound project."<br>"Create a scene called "Bridge". The session may not have a project bound yet —" |
| `scene_get_all` | [list-scenes](scenarios/sas/inspect/list-scenes.yaml)<br>[switch-to-chorus](scenarios/sas/scene/switch-to-chorus.yaml) | "What scenes do I have?"<br>"Go to the chorus." |
| `scene_activate` | [switch-to-chorus](scenarios/sas/scene/switch-to-chorus.yaml) | "Go to the chorus." |
| `project_get_status` | [scene-wrong-project-bound](scenarios/sas/scene/wrong-project-bound.yaml) | "Create a scene called "Bridge". The session may not have a project bound yet —" |
| `dsl_play` | [play-active-scene](scenarios/sas/transport/play.yaml) | "Play it." |
| `dsl_stop` | [stop-transport](scenarios/sas/transport/stop.yaml) | "Stop." |
| `dsl_set_tempo` | [scene-recovery-from-error](scenarios/sas/scene/recovery-from-error.yaml)<br>[set-tempo-128](scenarios/sas/transport/set-bpm.yaml) | "Set the project tempo to 128 BPM."<br>"Speed it up to 128." |
| `dsl_track_create` | [make-beat-bass-and-pads](scenarios/sas/make/bass-and-pads.yaml) | "Make me a beat with bass and pads." |
| `dsl_list_tracks` | [list-tracks](scenarios/sas/inspect/list-tracks.yaml) | "What tracks are in this project?" |
| `dsl_track_mute` | [mute-kick](scenarios/sas/track/mute-kick.yaml) | "Mute the kick." |
| `dsl_track_volume` | [lower-bass-volume](scenarios/sas/track/lower-bass.yaml) | "Make the bass quieter." |
| `dsl_track_pan` | [pan-bass-left](scenarios/sas/track/pan-bass-left.yaml) | "Pan the bass to the left." |
| `dsl_generate_midi` | [make-beat-bass-and-pads](scenarios/sas/make/bass-and-pads.yaml) | "Make me a beat with bass and pads." |
| `compose_scene` | [make-beat-bass-and-pads](scenarios/sas/make/bass-and-pads.yaml) | "Make me a beat with bass and pads." |
| `make_beat` | [make-beat-bass-and-pads](scenarios/sas/make/bass-and-pads.yaml) | "Make me a beat with bass and pads." |

## Uncovered actions (no scenario yet)

Each row is a candidate for a new scenario.

| Action | Description (truncated) | CLI form (if hand-written) |
| --- | --- | --- |
| `scene_add_track` | Move an existing track INTO a scene (doesn't create the track). Useful when a track was created without a scen… | `sas scene add-track` |
| `scene_set_mute` | Mute or unmute an entire scene (every track in it). Non-destructive; restores when you unmute. | `sas scene mute` |
| `scene_get_tracks` | List every track inside a specific scene (names as primary; ids as fallback). | `sas scene tracks` |
| `scene_move_track` | Move an existing track from its current scene (or the pool) into a different scene. Two-step engine operation:… | `sas scene move-track` |
| `scene_set_collapsed` | Set a scene's collapsed/expanded display state. Historical no-op in the Tracktion engine (there's nothing to c… | `sas scene set-collapsed` |
| `scene_queue` | Queue a scene switch to happen at the next bar boundary. Currently routes to scene_activate (immediate switch)… | `sas scene queue` |
| `scene_delete` | Delete a scene AND every track inside it. Deletes child tracks first to prevent ghost audio from orphaned audi… | `sas scene delete` |
| `scene_duplicate` | Duplicate a scene — produces an independent copy with new database UUIDs, new Tracktion engine tracks/folder, … | `sas scene duplicate` |
| `scene_find_by_name` | Resolve a scene name to its engine id. Used internally by other tools; agents rarely need this directly. | `sas scene find` |
| `scene_get_bars` | Read the loop length (in bars) of a scene. Per-scene bar lengths are 2, 4, 8, or 16. | `sas scene get-bars` |
| `scene_set_bars` | Change the loop length (in bars) of a scene. Allowed values are 2, 4, 8, or 16. | `sas scene set-bars` |
| `dsl_get_tempo_info` | Read the current project BPM from the engine (live value, not DB cache). | `sas transport tempo` |
| `dsl_track_delete` | Delete a single track from the project (engine + DB). | `sas track delete` |
| `dsl_track_solo` | Solo a single track (mutes every other track in the scene) or unsolo it (restore previous mute states). | `sas track solo` |
| `dsl_track_rename` | Rename a single track. Display-only — engine_track_id stays stable so playback and routing are unaffected. | `sas track rename` |
| `dsl_generate_drums` | Generate drum/percussion MIDI specifically — applies drum-specific quantization and kit mapping on top of the … | `sas generate drums` |
| `dsl_load_fx_chain` | Load a REAPER-format .RfxChain preset file onto a track (multi-plugin chain in one file). | `sas fx load` |
| `dsl_get_random_fx_chain` | Pick a random .RfxChain preset file from a preset-map category. Returns the file path; the caller (usually rac… | `sas fx random` |
| `dsl_get_preset_categories` | List the preset categories available under SAS_Preset_Map — the directories that rack_apply_random_fx and dsl_… | `sas fx categories` |
| `rack_set_instrument` | Load a specific instrument preset onto a track. Accepts a Surge XT preset path OR a .RfxChain file path. Repla… | `sas rack set-instrument` |
| `rack_apply_random_fx` | Apply a random preset to each of the 5 REAPER-bridge effect slots (EQ, Distortion, Delay, Reverb, Compression)… | `sas rack random-fx` |
| `CheckSurgeXTLoadable` | Probe whether the Surge XT VST3 can be loaded by the audio engine. Diagnostic / health check — does not modify… | `sas rack check-surge` |
| `dsl_set_track_fx` | Enable/disable one FX category on one track, and tune its preset and dry/wet mix. Non-destructive and instant.… | `sas fx set-track` |
| `dsl_get_track_fx` | Read the current FX state for a track: which categories are enabled, which preset (1-5) is active, and the dry… | `sas fx get-track` |
| `get_musical_context` | Return the active scene's musical context: key, BPM (live from engine), chord progression, and genre. | `sas context get` |
| `set_musical_context` | Update the active scene's BPM, key + chord progression, and/or genre. Any subset of fields is allowed; at leas… | `sas context set` |
| `set_scene_fx` | Apply one FX category change to every track in the active scene. Partial failures don't abort the batch — the … | `sas fx scene` |
| `export_audio` | Render a scene offline and save it as a WAV file at a user-specified path. Single-call answer to "export this … | `sas render export` |
| `search_samples` | Search the sample library by BPM, key, category, or text. Read-only. Returns a ranked match list with semantic… | `sas sample search` |
| `import_samples` | Import audio files (and/or whole directories) into the sample library. Hashes for dedup, analyzes BPM/key via … | `sas sample import` |
| `add_sample_track` | Add a sample from the library as a track in the active scene. Creates a DB record for the sample-track; the en… | `sas sample add` |
| `fit_sample_to_scene` | Fit a library sample to a scene's loop length and tempo. Time-stretches the sample to the project BPM, then ch… | `sas sample fit` |
| `process_audio` | Run a chain of audio operations on a WAV file via the bundled sas-audio-processor. File-in, file-out — does no… | `sas audio process` |
| `list_projects` | List every known project in the DB (id, name, path, last-opened timestamp). The currently-bound project is fla… | `sas project list` |
| `delete_transition` | Delete a transition row and orphan any tracks tied to it. Non-cascading — child tracks become orphaned (transi… | `sas transition delete` |
| `tool_search` | Search the tool registry for capabilities not loaded in your default tool list. Returns matching tool names, d… | `sas tools search` |
| `add_instrument` | Add a single AI-composed track to the active scene: creates the track, generates MIDI, rolls back on failure. … | `sas instrument add` |
| `generate_track` | One-call atomic primitive for "generate a bassline" / "give me drums" / "make me a melody". Bundles project ch… | `sas generate track` |
| `play_scene` | Start transport playback on the cue deck (loop-a / headphones). Gates on project-bound, scene-exists, scene-ha… | `sas scene play` |
| `render_to_performance` | Render a scene offline and play it on the main output (performance deck / loop-b / channels 3-4). Encapsulates… | `sas render performance` |
| `create_transition` | Create a musical transition between two scenes: auto-generates a 1-4-bar bridge with transition-specific percu… | `sas transition create` |
| `sas_inspect_project` | Read-only snapshot of the current project: scenes, track roster, | `sas inspect project` |
| `sas_inspect_scene` | Read-only snapshot of one scene: name, length in bars, key/mode/BPM, | `sas inspect scene` |
| `sas_inspect_track` | Read-only snapshot of one track: name, role, mute/solo/volume/pan, | `sas inspect track` |
| `sas_inspect_history` | List recent checkpoints (auto-created during apply, or explicit). | `sas inspect history` |
| `sas_create_plan` | Build a typed JSON Plan from a free-text intent OR a structured | `sas plan create` |
| `sas_validate_plan` | Validate a Plan against current project state. Returns | `sas validate` |
| `sas_apply_plan` | Execute a Plan. Auto-creates a checkpoint pre-apply (per | `sas apply` |
| `sas_render_preview` | Render an audio preview of a scene or single track. Returns | `sas preview` |
| `sas_history_checkpoint` | Manually create a named checkpoint of the current project state. | `sas history checkpoint` |
| `sas_history_undo` | Restore the project to a named checkpoint. Atomic — DB rows | `sas history undo` |
| `sas_history_list` | List checkpoints for the current project, newest first. | `sas history list` |
| `sas_history_prune` | Drop checkpoints whose expires_at has passed. Safe to call | `sas history prune` |
| `sas_history_delete` | Delete a single named checkpoint. Use sparingly — checkpoints | `sas history delete` |
| `revise_track` | Revise an existing track in place. Emits a typed track_revise Plan | `sas revise track` |
| `revise_scene` | Revise every track in a scene (or a scope-filtered subset) with | `sas revise scene` |
| `summarize_project` | Free-text summary of the current project — scene + track inventory, | `sas summarize project` |

